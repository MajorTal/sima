{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "BeliefRevisionEvent",
  "description": "HOT belief revision loop event tracking re-run of earlier modules",
  "type": "object",
  "additionalProperties": false,
  "required": ["iteration", "trigger_confidence", "threshold", "action"],
  "properties": {
    "iteration": {
      "type": "integer",
      "minimum": 1,
      "description": "Which belief revision iteration this is"
    },
    "trigger_confidence": {
      "type": "number",
      "minimum": 0.0,
      "maximum": 1.0,
      "description": "Confidence level that triggered the revision"
    },
    "threshold": {
      "type": "number",
      "minimum": 0.0,
      "maximum": 1.0,
      "description": "Confidence threshold below which revision is triggered"
    },
    "uncertainties": {
      "type": "array",
      "items": { "type": "string" },
      "description": "List of uncertainties from metacognition to resolve"
    },
    "action": {
      "type": "string",
      "description": "Action being taken to address the low confidence"
    }
  }
}
